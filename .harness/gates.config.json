{
  "gates": [
    { "name": "lint", "cmd": "python -m compileall -q src tests app.py", "blocking": true },
    { "name": "typecheck", "cmd": "python -m compileall -q src tests app.py", "blocking": true },
    { "name": "test", "cmd": "python -m unittest discover -s tests", "blocking": true },
    { "name": "diff-scope", "blocking": true }
  ]
}
