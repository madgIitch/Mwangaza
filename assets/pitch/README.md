# Final-build captures

The pitch uses a live three-click demo. These full-resolution captures from the final
build are retained as its offline visual fallback:

1. `01-landing-final.png` — `/landing`, hero and current coverage visible.
2. `02-overview-persistent-episodes.png` — `/overview?layer=episodes`, IGAD persistent
   episodes layer and legend visible.
3. `03-adm1-continuation-evidence.png` — an active ADM1 in `/region?layer=episodes`, with
   30/60/90/180-day controls and observation freshness visible.

The source browser screenshots are 1920×1020, retain the live source and effective
observation window, and contain no demo data.

