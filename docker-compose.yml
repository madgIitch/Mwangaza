services:
  api:
    image: mwangaza-api:local
    build:
      context: .
      target: api
    environment:
      MWANGAZA_ENV: demo
      MWANGAZA_MODE: demo
      MWANGAZA_API_DATA_MODE: demo
      MWANGAZA_CACHE_REQUIRED: "false"
      PORT: "8080"
    ports:
      - "18081:8080"
    healthcheck:
      test:
        - CMD
        - python
        - -c
        - "import urllib.request; urllib.request.urlopen('http://127.0.0.1:8080/ready', timeout=3)"
      interval: 5s
      timeout: 4s
      retries: 24
      start_period: 5s
    restart: unless-stopped

  refresh:
    profiles: ["refresh"]
    image: mwangaza-refresh:local
    build:
      context: .
      target: refresh
    environment:
      MWANGAZA_ENV: local
      MWANGAZA_MODE: production
      MWANGAZA_API_DATA_MODE: cache
      MWANGAZA_CACHE_DIR: /app/.cache/mwangaza
    volumes:
      - refresh-cache:/app/.cache/mwangaza
    command: ["--period", "2026-07-30"]

  web:
    image: mwangaza-web:local
    build:
      context: .
      target: web
    environment:
      API_UPSTREAM: http://api:8080
      PORT: "8080"
    ports:
      - "18080:8080"
    depends_on:
      api:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "wget", "-q", "-T", "3", "-O", "-", "http://127.0.0.1:8080/healthz"]
      interval: 5s
      timeout: 4s
      retries: 12
      start_period: 3s
    restart: unless-stopped

volumes:
  refresh-cache:
