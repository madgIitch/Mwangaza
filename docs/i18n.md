# Multilingual Interface

Sprint 38 adds essential interface translations for `en`, `sw` and `so`.

Translation logic lives in `mwangaza.i18n`. Missing keys fall back to English and
return a structured warning. `validate_catalogs()` fails if any required key is
missing from a catalog.

Dataset names, source identifiers, numeric values, ISO dates and API contracts
are not translated.
