# Alert Thresholds

Sprint 18 adds configurable prototype thresholds for later alert levels.

Allowed levels are `green`, `yellow`, `orange`, `red` and `unknown`. Presets must
cover the configured domain without gaps or overlaps. Blocked data quality or a
missing value forces `unknown`.

The default preset is explicitly labelled as prototype configuration and is not
an official IGAD standard.
